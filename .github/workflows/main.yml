name: Deploy Hexo

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install dependencies
      run: npm install

    - name: Generate Hexo static files
      run: |
        git checkout public  # 切换到 public 分支
        npx hexo generate
        echo "Generated files in public directory:"
        ls -la  # 确保 public 目录中有生成的文件

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./public
        cname: your-custom-domain.com  # 如果有自定义域名，请在此处填写
